{
  "name": "neural-render-src-shim",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix ../backend install",
    "start": "npm --prefix ../backend start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
